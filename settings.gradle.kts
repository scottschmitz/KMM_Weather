pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "KMM_Sample"
include(":androidApp")
include(":domain")
include(":data")
